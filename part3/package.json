{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:ui": "cd ../../phonebook && rm -rf dist && cd ../FSO/part2/phonebook && npm run build && cp -r dist ../../../phonebook && rm -rf dist",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
